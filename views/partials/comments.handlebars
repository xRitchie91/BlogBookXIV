{{!-- Partial for individual comment --}}
<section class="card">
    <h4>{{comment_text}}</h4>
    <section class="container">
        <p class="post-text">{{user.username}}</p>
    </div>
</div>